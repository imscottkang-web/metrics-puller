# Copy to `.env` (gitignored) to override defaults. Never commit real values.
#
# Path to the downloaded OAuth "Desktop app" client secret JSON.
# If unset, the tool looks for secrets/client_secret.json next to metrics.py.
YT_OAUTH_CLIENT_SECRET=/absolute/path/to/client_secret_XXXX.apps.googleusercontent.com.json
